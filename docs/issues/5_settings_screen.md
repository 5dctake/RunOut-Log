# Issue #5: 設定画面とデータ管理 [完了]

## 概要
データの削除機能やアプリ情報の表示を実装する。

## タスク
- [x] 種目表示（将来拡張用プレースホルダー）
- [x] データ全削除機能（確認ダイアログ付き）
- [x] クラウド同期（有料版ロック表示）
- [x] アプリバージョン表示

## 実装結果
- `SettingsScreen` にて各項目を `ListTile` で実装。
- データ全削除時に `AlertDialog` を表示し、誤操作を防止。
- `RecordRepository` および `RecordsNotifier` を介して Hive ボックスをクリアする機能を統合。
